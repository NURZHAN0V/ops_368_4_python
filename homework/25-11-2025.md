# Домашнее задание от 25‑11‑2025
## 3 задания для закрепления материала

---

**Задание 1. Создай файл `.env`**  
*Что нужно сделать:*  
1. Создай файл `.env` в папке проекта  
2. Добавь туда переменную `MY_SECRET` с любым значением (например, `MY_SECRET=hello123`)  
3. Создай файл `test_env.py` и напиши код, который:  
   - Читает переменную `MY_SECRET` из файла `.env`  
   - Выводит её значение на экран  

*Ожидаемый результат:*  
```
Мой секрет: hello123
```

*Подсказка:*  
```python
from dotenv import load_dotenv
import os

load_dotenv('.env')
# твой код здесь
```

*Цель:*  
Научиться работать с переменными окружения и файлом `.env`.

---

**Задание 2. Создай свою базу данных**  
*Что нужно сделать:*  
1. Создай файл `my_database.py`  
2. Подключись к базе данных `my_data.db` (файл создастся автоматически)  
3. Создай таблицу `books` с полями:  
   - `id` — уникальный номер (INTEGER PRIMARY KEY AUTOINCREMENT)  
   - `title` — название книги (TEXT NOT NULL)  
   - `author` — автор книги (TEXT NOT NULL)  

*Подсказка:*  
```python
import sqlite3

conn = sqlite3.connect('my_data.db')
cursor = conn.cursor()

cursor.execute('''
    CREATE TABLE IF NOT EXISTS books (
        # твой код здесь
    )
''')

conn.close()
```

*Цель:*  
Научиться создавать базу данных и таблицу самостоятельно.

---

**Задание 3. Добавь книгу в базу данных**  
*Что нужно сделать:*  
1. Напиши функцию `add_book(title, author)`, которая:  
   - Подключается к базе данных `my_data.db`  
   - Добавляет новую книгу в таблицу `books`  
   - Выводит сообщение: `"Книга [название] добавлена!"`  
   - Закрывает соединение с базой  

2. Вызови функцию 2 раза, добавив любые книги  

*Ожидаемый результат:*  
```
Книга Гарри Поттер добавлена!
Книга Властелин колец добавлена!
```

*Подсказка:*  
```python
def add_book(title, author):
    conn = sqlite3.connect('my_data.db')
    cursor = conn.cursor()
    
    cursor.execute('INSERT INTO books (title, author) VALUES (?, ?)', (title, author))
    # не забудь commit() и close()!

add_book("Гарри Поттер", "Дж. Роулинг")
add_book("Властелин колец", "Дж. Толкин")
```

*Цель:*  
Закрепить умение добавлять записи в базу данных.

---

**Советы по выполнению:**  
- Выполняй задания по порядку — каждое следующее опирается на предыдущее.  
- Не забывай про `conn.commit()` — без этого данные не сохранятся!  
- Если что-то не работает — проверь, установлена ли библиотека `python-dotenv` (`pip install python-dotenv`).

