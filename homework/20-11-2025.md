### Домашнее задание от 20-11-2025
## Закрепление работы с классами

**Задание 1. Добавь новое поле в класс Client**

*Что нужно сделать:*  
Расширь класс `Client`, добавив новое поле `email` (электронная почта). Теперь каждый клиент должен иметь не только ID, имя, телефон и Telegram, но и email.

*Требования:*  
1. В файле `clients.py` обнови класс `Client`: добавь параметр `email` в функцию `__init__` и сохрани его в `self.email`.  
2. Обнови функцию `client_read()`, чтобы она выводила email каждого клиента.  
3. Обнови функцию `client_add()`, чтобы при добавлении нового клиента программа запрашивала email у пользователя и передавала его при создании объекта.  
4. Обнови функцию `client_edit()`, добавив в меню пункт "4 - изменить email" и возможность редактирования этого поля.

*Пример обновлённого класса:*
```python
class Client:
    def __init__(self, id, name, phone, telegram, email):
        self.id = id
        self.name = name
        self.phone = phone
        self.telegram = telegram
        self.email = email
```

*Пример обновлённого вывода:*
```python
def client_read():
    print("Список клиентов:👯\n")
    for client in client_list:
        print(
            f"ID: {client.id}\n"
            f"Имя: {client.name}\n"
            f"Телефон: {client.phone}\n"
            f"Телеграм: {client.telegram}\n"
            f"Email: {client.email}\n"
        )
```

*Подсказки:*  
- Не забудь обновить все места, где создаётся объект `Client`, добавив параметр `email`.  
- При редактировании email используй `user.email = input("Email: ")` — так же, как мы делали с другими полями.  
- Если в твоём коде есть тестовые данные (например, в `test.py`), их тоже нужно обновить.

*Цель:*  
Научиться расширять классы, добавляя новые поля, и обновлять все связанные функции для работы с новыми данными.

---

**Задание 2. Исправь старые функции для работы с классами**

*Что нужно сделать:*  
Проверь все функции в файле `clients.py` и убедись, что они используют атрибуты объектов (`client.id`, `client.name` и т.д.), а не индексы списков (`client[0]`, `client[1]`). Если найдёшь места, где ещё используются индексы — исправь их.

*Требования:*  
1. Пройдись по всем функциям: `client_read()`, `client_add()`, `client_remove()`, `client_edit()`.  
2. Убедись, что везде используются атрибуты объектов, а не индексы.  
3. Особое внимание удели функции `client_edit()` — там могли остаться старые конструкции вроде `client[0]` или `user[0]`.  
4. Проверь, что проверка существования клиента использует `user is not None` вместо `len(user) > 0`.

*Что искать и исправлять:*
- `client[0]` → заменить на `client.id`
- `client[1]` → заменить на `client.name`
- `client[2]` → заменить на `client.phone`
- `client[3]` → заменить на `client.telegram`
- `len(user) > 0` → заменить на `user is not None`
- `user = []` → заменить на `user = None`
- Создание нового списка для замены объекта → заменить на прямое изменение атрибутов

*Пример исправления:*
```python
# БЫЛО (неправильно для объектов):
for client in client_list:
    if client[0] == user_id:
        user = client
        break

if len(user) > 0:
    client_list[user_index] = [
        user[0],
        input("Имя: "),
        user[2],
        user[3]
    ]

# СТАЛО (правильно для объектов):
for client in client_list:
    if client.id == user_id:
        user = client
        break

if user is not None:
    user.name = input("Имя: ")
```

*Подсказки:*  
- Запусти программу и попробуй выполнить все действия (добавить, посмотреть, отредактировать, удалить клиента). Если увидишь ошибки — значит, где-то остался старый код.  
- Ошибки типа `TypeError: 'Client' object is not subscriptable` означают, что ты пытаешься использовать индексы (`[0]`, `[1]`) с объектом класса.

*Цель:*  
Закрепить понимание разницы между списками и объектами классов, научиться правильно работать с атрибутами объектов.

---

**Задание 3. Создай функцию поиска клиента по имени**

*Что нужно сделать:*  
Создай новую функцию `client_search()` в файле `clients.py`, которая будет искать клиентов по имени (или части имени) и выводить информацию о найденных клиентах.

*Требования:*  
1. Функция должна запрашивать у пользователя имя (или часть имени) для поиска.  
2. Пройдись по всем клиентам в `client_list` и найди тех, чьё имя содержит введённую подстроку (используй оператор `in`).  
3. Выведи информацию о всех найденных клиентах в том же формате, что и в `client_read()` (ID, имя, телефон, Telegram, email).  
4. Если клиентов не найдено — выведи понятное сообщение: `"Клиенты с именем «...» не найдены."`  
5. Добавь эту функцию в меню в `main.py` как пункт "5 - найти клиента по имени".

*Пример кода функции:*
```python
def client_search():
    print("Поиск клиента 🔍\n")
    search_name = input("Введите имя для поиска: ")
    found_clients = []
    
    for client in client_list:
        if search_name.lower() in client.name.lower():
            found_clients.append(client)
    
    if len(found_clients) > 0:
        print(f"\nНайдено клиентов: {len(found_clients)}\n")
        for client in found_clients:
            print(
                f"ID: {client.id}\n"
                f"Имя: {client.name}\n"
                f"Телефон: {client.phone}\n"
                f"Телеграм: {client.telegram}\n"
                f"Email: {client.email}\n"
            )
    else:
        print(f"Клиенты с именем «{search_name}» не найдены.")
```

*Детали реализации:*  
- `search_name.lower()` и `client.name.lower()` — приводим строки к нижнему регистру, чтобы поиск был нечувствителен к регистру (найдёт "Иван" даже если искать "иван").  
- `found_clients` — список для хранения найденных клиентов.  
- `len(found_clients)` — количество найденных клиентов.

*Добавление в меню (main.py):*
```python
print(
    "1 - посмотреть всех клиентов\n"
    "2 - добавить нового клиента\n"
    "3 - удалить клиента из списка\n"
    "4 - редактировать клиента из списка\n"
    "5 - найти клиента по имени\n"
)
# ...
elif value == "5":
    print()  # пустая строка
    client_search()
    print()  # пустая строка
```

*Подсказки:*  
- Используй `in` для проверки, содержится ли подстрока в имени: `if "иван" in "Иван Петров"` вернёт `True`.  
- Можно сделать поиск более гибким: искать не только по имени, но и по телефону или Telegram. Это будет дополнительным заданием, если хочешь усложнить.

*Цель:*  
Научиться создавать новые функции для работы с объектами классов, использовать поиск по строкам и выводить результаты в удобном формате.

---

**Советы по выполнению:**  
- Начинай с первого задания — оно самое простое и поможет лучше понять структуру класса.  
- Для второго задания запусти программу и протестируй все функции. Если что-то не работает — проверь, не осталось ли где-то старых индексов.  
- Для третьего задания сначала напиши функцию и протестируй её отдельно, затем добавь в меню.  
- Используй `print()` для отладки — выводи промежуточные значения, чтобы понять, что происходит в коде.  
- Помни: с объектами классов работаем через атрибуты (`client.name`), а не через индексы (`client[1]`).  
- Если видишь ошибку `'Client' object is not subscriptable` — значит, где-то используется старый способ обращения к данным через индексы.

