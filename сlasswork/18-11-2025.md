# Работа на занятии 18‑11‑2025  
## Редактирование клиентов и авторизация

### 1. Проверка существования клиента перед редактированием

**Зачем это нужно:**  
Перед тем как редактировать клиента, нужно убедиться, что он действительно есть в базе. Если клиента с таким ID нет, программа должна сообщить об этом и не пытаться что-то изменить.

**Что делаем:**  
Добавляем проверку: если клиент не найден, выводим сообщение об ошибке и прекращаем выполнение функции с помощью `return`.

**Как это работает:**  
1. Если клиент не найден в списке, переменная `user` остаётся пустой (пустой список `[]`).  
2. Проверяем длину списка `user` — если она равна 0, значит клиента нет.  
3. Выводим сообщение "Такого клиента нет в базе" и выходим из функции.

**Код:**  
```python
        return

print("Такого клиента нет в базе")
```

**Результат:**  
Программа не будет пытаться редактировать несуществующего клиента и сообщит пользователю об ошибке.

---

### 2. Создание функции редактирования клиента

**Зачем это нужно:**  
Нужна возможность изменять данные уже существующих клиентов — исправлять имя, телефон или Telegram, если они изменились.

**Что делаем:**  
Создаём функцию `client_edit()`, которая находит клиента по ID и позволяет изменить все его данные сразу.

**Как это работает:**  
1. Создаём пустой список `user` для хранения найденного клиента.  
2. Просим пользователя ввести ID клиента, которого нужно отредактировать.  
3. Проходим по всем клиентам в списке и ищем того, у кого ID совпадает с введённым.  
4. Если нашли — сохраняем его в переменную `user` и выходим из цикла с помощью `break`.  
5. Находим позицию (индекс) этого клиента в списке с помощью `index()`.  
6. Заменяем весь список клиента новыми данными, которые вводит пользователь.

**Код:**  
```python
def client_edit():
        user = []
        user_id = int(input("Введите ID клиента: "))

        for client in client_list:
                if client[0] == user_id:
                        user = client
                        break

        # получаем индекс клиента
        user_index = client_list.index(user)

        # полностью редактируем клиента
        client_list[user_index] = [
                user[0],
                input("Имя: "),
                input("Телефон: "),
                input("Телеграм: ")
        ]
```

**Детали реализации:**  
- `user[0]` — сохраняем старый ID, его менять не нужно.  
- `input()` — запрашиваем новые данные у пользователя.  
- `client_list[user_index] = [...]` — заменяем весь список клиента новыми данными.

**Результат:**  
Можно найти клиента по ID и изменить все его данные за один раз.

---

### 3. Добавление проверки перед редактированием

**Зачем это нужно:**  
Если пользователь ввёл несуществующий ID, программа не должна пытаться редактировать пустого клиента. Нужно сначала проверить, был ли клиент найден.

**Что делаем:**  
Добавляем проверку: если список `user` не пустой (его длина больше 0), значит клиент найден и можно редактировать. Если пустой — выводим сообщение об ошибке.

**Как это работает:**  
1. Создаём пустой список `user` и просим пользователя ввести ID клиента.  
2. Ищем клиента в списке по ID — если находим, сохраняем его в `user`.  
3. Проверяем длину списка `user` с помощью `len(user) > 0`.  
4. Если длина больше 0 — клиент найден, выполняем редактирование.  
5. Если длина равна 0 — клиента нет, выводим сообщение об ошибке.

**Код:**  
```python
def client_edit():
    user = []
    user_id = int(input("Введите ID клиента: "))

    for client in client_list:
        if client[0] == user_id:
            user = client
            break

    if len(user) > 0:
        # получаем индекс клиента
        user_index = client_list.index(user)

        # полностью редактируем клиента
        client_list[user_index] = [
            user[0],
            input("Имя: "),
            input("Телефон: "),
            input("Телеграм: ")
        ]
    else:
        print("Такого киента нет в базе")
```

**Результат:**  
Программа безопасно обрабатывает ситуацию, когда клиента с таким ID не существует, и не выдаёт ошибку.

---

### 4. Меню выбора поля для редактирования

**Зачем это нужно:**  
Иногда нужно изменить только одно поле (например, только телефон), а не все данные сразу. Удобнее дать пользователю выбор, что именно он хочет изменить.

**Что делаем:**  
Создаём меню, где пользователь может выбрать, какое поле он хочет отредактировать: имя, телефон или Telegram.

**Как это работает:**  
1. Выводим список вариантов с номерами.  
2. Просим пользователя ввести номер выбранного пункта.  
3. Сохраняем выбор в переменную `value`.  
4. Добавляем пустую строку для красоты вывода.

**Код:**  
```python
print(
        "1 - изменить имя\n"
        "2 - изменить номер телефона\n"
        "3 - изменить телеграм\n"
)
value = input("Вы: ")
print() # пустая строка
```

**Результат:**  
Пользователь видит понятное меню и может выбрать, что именно хочет изменить.

---

### 5. Редактирование отдельных полей

**Зачем это нужно:**  
Теперь вместо изменения всех данных сразу, можно менять только то, что нужно. Например, если изменился только телефон, не нужно заново вводить имя и Telegram.

**Что делаем:**  
В зависимости от выбора пользователя, меняем только одно поле, а остальные оставляем без изменений.

**Как это работает:**  
1. Если выбрали "1" — меняем только имя, остальные поля (`user[2]` — телефон, `user[3]` — Telegram) оставляем как были.  
2. Если выбрали "2" — меняем только телефон, имя и Telegram оставляем.  
3. Если выбрали "3" — меняем только Telegram, имя и телефон оставляем.  
4. Если ввели что-то другое — сообщаем, что команда не распознана.

**Код:**  
```python

if value == "1":
        client_list[user_index] = [
        user[0],
        input("Имя: "),
        user[2],
        user[3]
        ]
elif value == "2":
        client_list[user_index] = [
        user[0],
        user[1],
        input("Телефон: "),
        user[3]
        ]
elif value == "3":
        client_list[user_index] = [
        user[0],
        user[1],
        user[2],
        input("Телеграм: ")
        ]
else:
        print("Команда не распознана 🤖")
        print("Попробуйте еще раз\n")
```

**Детали реализации:**  
- `user[0]` — ID всегда остаётся прежним.  
- `user[1]`, `user[2]`, `user[3]` — старые значения полей, которые мы сохраняем, если их не меняем.  
- `input()` — запрашиваем только новое значение для выбранного поля.

**Результат:**  
Можно быстро изменить только нужное поле, не вводя заново все остальные данные.

---

### 6. Создание системы авторизации

**Зачем это нужно:**  
Чтобы защитить программу от посторонних, нужно добавить вход по логину и паролю. Только тот, кто знает правильные данные, сможет использовать программу.

**Что делаем:**  
Создаём отдельный файл `auth.py` с функцией входа в систему. В нём храним правильный логин и пароль, а функция проверяет, совпадают ли введённые данные.

**Как это работает:**  
1. Сохраняем правильный логин и пароль в переменные `login_admin` и `password_admin`.  
2. Функция `login()` просит пользователя ввести логин и пароль.  
3. Сравниваем введённые данные с правильными.  
4. Если всё совпадает — возвращаем `True` (истина, вход разрешён).  
5. Если не совпадает — возвращаем `False` (ложь, вход запрещён).

**Код в `auth.py`:**  
```python
login_admin = "admin"
password_admin = "admin"

def login():
    login = input("Логин: ")
    password = input("Пароль: ")

    if login_admin == login and password_admin == password:
        return True
    else:
        return False
```

**Детали реализации:**  
- `login_admin` и `password_admin` — правильные данные для входа.  
- `login` и `password` — то, что ввёл пользователь.  
- `return True` — означает, что вход успешен.  
- `return False` — означает, что данные неверные, вход запрещён.

**Результат:**  
Программа теперь защищена — без правильного логина и пароля в неё не войти.

---

### 7. Подключение авторизации к основной программе

**Зачем это нужно:**  
Нужно использовать функцию входа в основном файле программы, чтобы проверять авторизацию перед запуском основного меню.

**Что делаем:**  
В файле `main.py` вызываем функцию `login()` и проверяем её результат. Если вход успешен — запускаем программу, если нет — сообщаем об ошибке.

**Как это работает:**  
1. Вызываем функцию `login()` и сохраняем результат в переменную `status_auth`.  
2. Если `status_auth` равен `True` — значит вход успешен, выводим приветствие и запускаем программу.  
3. Если `status_auth` равен `False` — значит данные неверные, выводим сообщение об ошибке.  
4. Основной цикл программы (`while status_auth`) будет работать только если `status_auth` равен `True`.

**Код в `main.py`:**  
```python
status_auth = login()

if status_auth:
    print("Вы успешно вошли в программу!")
else:
    print("Логин или пароль неверный")

while status_auth:
        # тело программы
```

**Детали реализации:**  
- `status_auth` — переменная, которая хранит результат проверки (True или False).  
- `while status_auth` — цикл будет работать, пока `status_auth` равен `True`.  
- Если вход не удался, цикл не запустится, и программа завершится.

**Результат:**  
Программа теперь требует ввода логина и пароля перед началом работы. Без правильных данных доступ к программе закрыт.
