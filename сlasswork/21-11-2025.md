# Работа на занятии 21‑11‑2025
## Редактирование клиента и работа с классами

### 1. Переход от списков к классам

**Что изменилось:**  
Раньше мы хранили клиентов в виде списков: `[1, "Олег", "+7 993 565 45 78", "@olegnastyle"]`. Теперь мы используем класс `Client` — это более удобный способ работы с данными.

**Зачем это нужно:**  
С классами проще обращаться к данным. Вместо `client[0]` (непонятно, что это) мы пишем `client.name` (сразу ясно — это имя). Код становится читабельнее и понятнее.

**Как это работает:**  
Класс — это шаблон для создания объектов. Мы создаём класс `Client` с полями: `id`, `name`, `phone`, `telegram`. Когда нужно создать клиента, мы используем этот шаблон.

**Код класса:**  
```python
class Client:
    def __init__(self, id, name, phone, telegram):
        self.id = id
        self.name = name
        self.phone = phone
        self.telegram = telegram
```

**Что означает:**  
- `class Client:` — создаём класс с именем "Client"  
- `def __init__():` — это специальная функция, которая вызывается при создании нового клиента  
- `self.id = id` — сохраняем переданный ID в поле объекта  
- `self.name = name` — сохраняем переданное имя в поле объекта  
- То же самое для телефона и телеграма

**Как создать клиента:**  
```python
new_client = Client(
    id=1,
    name="Олег",
    phone="+7 993 565 45 78",
    telegram="@olegnastyle"
)
```

**Как обратиться к данным:**  
```python
print(new_client.name)    # выведет: Олег
print(new_client.phone)   # выведет: +7 993 565 45 78
```

---

### 2. Функция редактирования клиента

**Что делаем:**  
Создаём функцию `client_edit()`, которая позволяет изменить данные существующего клиента: имя, телефон или телеграм.

**Как это работает:**  
1. Пользователь вводит ID клиента, которого хочет отредактировать.  
2. Программа ищет клиента с таким ID в списке.  
3. Если клиент найден — показываем меню выбора, что именно изменить.  
4. Пользователь выбирает, что менять, и вводит новые данные.  
5. Данные обновляются в объекте клиента.

**Полный код функции:**  
```python
def client_edit():
    user = None
    user_id = int(input("Введите ID клиента: "))

    # проверяем на существование
    for client in client_list:
        if client.id == user_id:
            user = client
            break

    if user:
        print(
            "1 - изменить имя\n"
            "2 - изменить номер телефона\n"
            "3 - изменить телеграм\n"
        )
        value = input("Вы: ")
        print() # пустая строка
        if value == "1":
            user.name = input("Имя: ")
        elif value == "2":
            user.phone = input("Телефон: ")
        elif value == "3":
            user.telegram = input("Телеграм: ")
        else:
            print("Команда не распознана 🤖")
            print("Попробуйте еще раз\n")
    else:
        print("Такого клиента нет в базе")
```

---

### 3. Пошаговое объяснение функции

#### Шаг 1: Инициализация переменной

**Что делаем:**  
Создаём переменную `user` и присваиваем ей значение `None` (ничего).

**Код:**  
```python
user = None
```

**Зачем это нужно:**  
Переменная `user` будет хранить найденного клиента. Пока мы его не нашли, она равна `None`. Это нужно, чтобы потом проверить, нашли ли мы клиента или нет.

**Что такое `None`:**  
`None` — это специальное значение в Python, которое означает "ничего" или "пусто". Это не ноль и не пустая строка — это именно отсутствие значения.

---

#### Шаг 2: Запрос ID у пользователя

**Что делаем:**  
Просим пользователя ввести ID клиента, которого он хочет отредактировать.

**Код:**  
```python
user_id = int(input("Введите ID клиента: "))
```

**Как это работает:**  
- `input("Введите ID клиента: ")` — выводит сообщение и ждёт ввода от пользователя  
- `int(...)` — преобразует введённый текст в число (ID всегда число)  
- `user_id = ...` — сохраняем это число в переменную

**Пример:**  
Если пользователь введёт "3", то `user_id` будет равен числу 3.

---

#### Шаг 3: Поиск клиента в списке

**Что делаем:**  
Проходим по всем клиентам в списке и ищем того, у которого ID совпадает с введённым.

**Код:**  
```python
for client in client_list:
    if client.id == user_id:
        user = client
        break
```

**Как это работает:**  
- `for client in client_list:` — перебираем каждого клиента в списке  
- `if client.id == user_id:` — проверяем, совпадает ли ID клиента с тем, что ввёл пользователь  
- `user = client` — если совпадает, сохраняем этого клиента в переменную `user`  
- `break` — прекращаем поиск, так как уже нашли нужного клиента

**Почему `break` важен:**  
Когда мы нашли нужного клиента, нет смысла продолжать искать дальше. `break` останавливает цикл и экономит время.

**Что такое `client.id`:**  
Это обращение к полю `id` объекта `client`. Если раньше мы писали `client[0]`, то теперь пишем `client.id` — так понятнее.

---

#### Шаг 4: Проверка, найден ли клиент

**Что делаем:**  
Проверяем, нашли ли мы клиента. Если `user` не равен `None`, значит клиент найден.

**Код:**  
```python
if user:
    # показываем меню и редактируем
else:
    print("Такого клиента нет в базе")
```

**Как это работает:**  
- `if user:` — проверяет, есть ли что-то в переменной `user`  
- Если `user` не равен `None`, условие считается истинным (True)  
- Если `user` равен `None`, условие считается ложным (False)

**Почему это работает:**  
В Python значение `None` считается "ложным", а любой объект (например, наш клиент) считается "истинным". Поэтому `if user:` проверяет, нашли ли мы клиента.

---

#### Шаг 5: Меню выбора поля для редактирования

**Что делаем:**  
Если клиент найден, показываем пользователю меню с вариантами, что можно изменить.

**Код:**  
```python
if user:
    print(
        "1 - изменить имя\n"
        "2 - изменить номер телефона\n"
        "3 - изменить телеграм\n"
    )
    value = input("Вы: ")
```

**Как это работает:**  
- `print(...)` — выводит меню на экран  
- `\n` — это символ переноса строки, каждая опция будет на новой строке  
- `input("Вы: ")` — ждёт, когда пользователь введёт свой выбор (1, 2 или 3)  
- `value = ...` — сохраняем выбор пользователя

**Результат на экране:**  
```
1 - изменить имя
2 - изменить номер телефона
3 - изменить телеграм
Вы: 
```

---

#### Шаг 6: Редактирование выбранного поля

**Что делаем:**  
В зависимости от выбора пользователя (1, 2 или 3) изменяем соответствующее поле у найденного клиента.

**Код:**  
```python
if value == "1":
    user.name = input("Имя: ")
elif value == "2":
    user.phone = input("Телефон: ")
elif value == "3":
    user.telegram = input("Телеграм: ")
else:
    print("Команда не распознана 🤖")
    print("Попробуйте еще раз\n")
```

**Как это работает:**  
- `if value == "1":` — если пользователь выбрал "1"  
  - `user.name = input("Имя: ")` — спрашиваем новое имя и сохраняем его в поле `name` объекта `user`  
- `elif value == "2":` — если пользователь выбрал "2"  
  - `user.phone = input("Телефон: ")` — спрашиваем новый телефон и сохраняем его  
- `elif value == "3":` — если пользователь выбрал "3"  
  - `user.telegram = input("Телеграм: ")` — спрашиваем новый телеграм и сохраняем его  
- `else:` — если пользователь ввёл что-то другое (не 1, не 2, не 3)  
  - Выводим сообщение об ошибке

**Почему это важно:**  
Когда мы изменяем `user.name`, мы изменяем поле объекта, который находится в списке `client_list`. Это значит, что изменения сохраняются сразу — нам не нужно искать клиента в списке заново и заменять его.

**Пример работы:**  
1. Пользователь вводит ID: `2`  
2. Находится клиент с ID=2 (например, Дмитрий)  
3. Пользователь выбирает "1" (изменить имя)  
4. Пользователь вводит новое имя: "Дмитрий Иванов"  
5. Теперь `user.name` (и соответственно `client_list[1].name`) равен "Дмитрий Иванов"

---

### 4. Преимущества использования классов

**Раньше (со списками):**  
```python
client = [1, "Олег", "+7 993 565 45 78", "@olegnastyle"]
print(client[0])  # что это? ID? Непонятно!
client[1] = "Олег Петров"  # меняем имя, но не очевидно
```

**Теперь (с классами):**  
```python
client = Client(1, "Олег", "+7 993 565 45 78", "@olegnastyle")
print(client.id)      # ясно — это ID
client.name = "Олег Петров"  # очевидно — меняем имя
```

**Почему это лучше:**  
- Код читается как обычный текст: `client.name` понятнее, чем `client[1]`  
- Меньше ошибок: нельзя случайно перепутать порядок полей  
- Проще редактировать: не нужно помнить, что `client[1]` — это имя, а `client[2]` — телефон

---

### 5. Итоговая структура функции

**Что мы узнали:**  
1. Как использовать классы вместо списков для хранения данных  
2. Как искать объект в списке по определённому полю (ID)  
3. Как изменять поля объекта напрямую  
4. Как проверять, найден ли объект, используя `None`  
5. Как создавать меню выбора для пользователя

**Главное правило:**  
Когда мы находим объект и сохраняем его в переменную (например, `user = client`), мы работаем с тем же самым объектом, что и в списке. Поэтому изменения в `user` автоматически отражаются в `client_list`.

**Результат:**  
Теперь мы можем легко редактировать данные любого клиента, просто указав его ID и выбрав, что именно нужно изменить.
