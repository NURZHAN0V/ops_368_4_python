# Работа на занятии 14‑11‑2025  
## Улучшаем программу  

### 1. Улучшим вывод клиентов  

**Что делаем:**  
Меняем формат вывода списка — теперь информация о каждом клиенте будет показана подробно и понятно, с заголовками для каждого поля.  

**Как это работает:**  
- Сначала печатаем приветственную строку с иконкой.  
- Затем проходим по всем клиентам в списке `client_list`.  
- Для каждого клиента выводим: ID, имя, телефон — каждое на отдельной строке.  

**Код:**  
```python
print("Список клиентов:👯\n")
for client in client_list:
    print(
        f"ID: {client[0]}\n"
        f"Имя: {client[1]}\n"
        f"Телефон: {client[2]}\n"
    )
```

**Результат:**  
Вы увидите аккуратный список, где данные каждого клиента разделены пустыми строками — так проще читать.

---

### 2. Новое поле ID для клиента  

**Зачем это нужно:**  
ID — это уникальный номер клиента. С ним легче находить, редактировать или удалять записи.  

**Что делаем:**  
- Добавляем ID в начало каждой записи в списке клиентов.  
- Обновляем вывод, чтобы показывать новое поле «Телеграм».  

**Код (обновлённый список):**  
```python
client_list = [
    [1, "Олег", "+7 993 565 45 78", "@olegnastyle"],
    [2, "Дмитрий", "+7 993 657 45 78", "@dima"],
    [3, "Владимир", "+7 993 897 45 78", "@vova"]
]
```

**Код (обновлённый вывод):**  
```python
print("Список клиентов:👯\n")
for client in client_list:
    print(
        f"ID: {client[0]}\n"
        f"Имя: {client[1]}\n"
        f"Телефон: {client[2]}\n"
        f"Телеграм: {client[3]}\n"
    )
```

**Результат:**  
Теперь у каждого клиента есть свой номер (ID), а также отображается его Telegram‑никнейм.

---

### 3. Обновим добавление клиентов  

**Что делаем:**  
При добавлении нового клиента автоматически присваиваем ему следующий по порядку ID (на 1 больше, чем у последнего).  

**Как это работает:**  
1. Узнаём длину списка клиентов (`len(client_list)`).  
2. Прибавляем 1 — получаем новый ID.  
3. Спрашиваем у пользователя имя, телефон и Telegram.  
4. Добавляем новую запись в список с новым ID.  
5. Сообщаем, что клиент успешно добавлен.  

**Код:**  
```python
print("Добавление клиента 👨‍👧\n")
user_last_id = len(client_list)+1
user_name = input("Имя: ")
user_phone = input("Номер телефона: ")
user_telegram = input("Телеграм: ")
client_list.append([
    user_last_id,
    user_name,
    user_phone,
    user_telegram
])
print(f"Клиент {user_name} был добавлен ✔️")
```

**Результат:**  
Новый клиент появляется в списке с правильным ID, а вы видите подтверждение добавления.

---

### 4. Удаление клиента  

**Что делаем:**  
Позволяем пользователю удалить клиента по его ID.  

**Как это работает:**  
1. Просим ввести ID клиента.  
2. Проходим по списку и ищем клиента с таким ID.  
3. Если нашли — удаляем его из списка и сообщаем об этом.  

**Код:**  
```python
print("Удаление клиента 👤\n")
user_id_remove = int(input("Введите ID клиента: "))
user_name_remove = ""
for client in client_list:
    if client[0] == user_id_remove:
        client_list.remove(client)
        user_name_remove = client[1]
        print(f"Клиент {user_name_remove} был удален ❌")
```

**Результат:**  
Клиент с указанным ID исчезает из списка, а вы получаете уведомление.

---

### 5. Реструктуризация проекта  

**Что это значит:**  
Мы разберём программу на части, чтобы её было проще читать, исправлять и дополнять.  

**Как это делаем:**  
1. Создаём отдельный файл `clients.py`, куда переносим список клиентов и функции для работы с ними.  
2. В `clients.py` создаём три функции:  
   - `client_add()` — добавляет нового клиента.  
   - `client_read()` — выводит список клиентов.  
   - `client_remove()` — удаляет клиента по ID.  
3. В основном файле `main.py` оставляем только меню и вызовы этих функций.  
4. Подключаем функции из `clients.py` с помощью `from clients import *`.  

**Код в `clients.py`:**  
```python
# clients.py
client_list = [
    [1, "Олег", "+7 993 565 45 78", "@olegnastyle"],
    [2, "Дмитрий", "+7 993 657 45 78", "@dima"],
    [3, "Владимир", "+7 993 897 45 78", "@vova"]
]

def client_add():
    print("Список клиентов:👯\n")
    for client in client_list:
        print(
            f"ID: {client[0]}\n"
            f"Имя: {client[1]}\n"
            f"Телефон: {client[2]}\n"
            f"Телеграм: {client[3]}\n"
        )

def client_read():
    print("Добавление клиента 👨‍👧\n")
    user_last_id = len(client_list)+1
    user_name = input("Имя: ")
    user_phone = input("Номер телефона: ")
    user_telegram = input("Телеграм: ")
    client_list.append([
        user_last_id,
        user_name,
        user_phone,
        user_telegram
    ])
    print(f"Клиент {user_name} был добавлен ✔️")

def client_remove():
    print("Удаление клиента 👤\n")
    user_id_remove = int(input("Введите ID клиента: "))
    user_name_remove = ""
    for client in client_list:
        if client[0] == user_id_remove:
            client_list.remove(client)
            user_name_remove = client[1]
            print(f"Клиент {user_name_remove} был удален ❌")
```

**Код в `main.py` (вызов функций):**  
```python
if value == "1":
    print() # пустая строка
    client_add()
    print() # пустая строка
elif value == "2":
    print() # пустая строка
    client_read()
    print() # пустая строка
elif value == "3":
    print() # пустая строка
    client_remove()
    print() # пустая строка
```

**Подключение функций:**  
```python
from clients import *
```

**Почему это полезно:**  
- Код становится чище и понятнее.  
- Если нужно что‑то исправить или добавить — ищем нужное в `clients.py`.  
- В `main.py` остаётся только логика меню.

---

### 6. Выход из программы с помощью `break`

**Зачем это нужно:**  
Команда `break` позволяет корректно завершить работу программы, выйдя из цикла меню. Это даёт пользователю возможность закрыть приложение в любой момент.

**Что делаем:**  
Добавляем опцию выхода из программы в меню и используем `break` для завершения цикла, когда пользователь выбирает эту опцию.

**Как это работает:**  
1. В меню добавляем пункт для выхода (например, "0" или "4").  
2. При выборе этого пункта выполняем команду `break`.  
3. Цикл завершается, и программа корректно закрывается.

**Код:**  
```python
else:
    print("Выход из программы 👋")
    break
```

**Результат:**  
Пользователь может безопасно завершить работу программы, выбрав соответствующий пункт меню, вместо принудительного закрытия через Ctrl+C.