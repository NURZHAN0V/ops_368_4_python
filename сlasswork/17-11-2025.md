# Работа на занятии 17‑11‑2025  
## Защита от дубликатов и улучшение логики

### 1. Проверка на дубликаты телефонов при добавлении клиента

**Зачем это нужно:**  
Предотвращаем добавление клиентов с одинаковыми номерами телефонов. Это важно, чтобы в базе не было повторяющихся записей и каждый номер был уникальным.

**Что делаем:**  
Перед добавлением нового клиента проверяем, не существует ли уже клиент с таким же номером телефона. Если такой клиент найден — сообщаем об этом и не добавляем дубликат.

**Как это работает:**  
1. После того как пользователь ввёл имя, телефон и Telegram, начинаем проверку.  
2. Проходим по всем существующим клиентам в списке `client_list`.  
3. Сравниваем введённый телефон (`user_phone`) с телефоном каждого клиента (`client[2]`).  
4. Если находим совпадение — выводим сообщение о том, что клиент с таким телефоном уже есть, и выходим из функции с помощью `return`.  
5. Если совпадений нет — добавляем нового клиента в список.

**Код:**  
```python
def client_add():
    print("Добавление клиента 👨‍👧\n")

    user_last_id = len(client_list) + 1
    user_name = input("Имя: ")
    user_phone = input("Номер телефона: ")
    user_telegram = input("Телеграм: ")

    for client in client_list:
        if client[2] == user_phone:
            print(f"{client[1]} уже есть в базе")
            return
      
    client_list.append([
        user_last_id,
        user_name,
        user_phone,
        user_telegram
    ])
    print(f"Клиент {user_name} был добавлен ✔️")
```

**Детали реализации:**  
- `client[2]` — это телефон клиента (третий элемент в списке, индекс 2).  
- `return` — прерывает выполнение функции, не давая коду ниже выполниться.  
- Проверка происходит до добавления в список, чтобы не создавать лишних записей.

**Результат:**  
При попытке добавить клиента с уже существующим номером телефона программа сообщит об этом и не создаст дубликат. Это помогает поддерживать целостность данных.
