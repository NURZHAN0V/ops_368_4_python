# Работа на занятии 20‑11‑2025  
## Переход от списков к классам

### 1. Создание класса Client

**Зачем это нужно:**  
Раньше мы хранили данные клиента в обычном списке: `[1, "Иван", "+7 996 564 87 89", "@ivenivanov"]`. Чтобы получить имя, нужно было писать `client[1]` — это не очень понятно. Класс позволяет дать каждому полю понятное имя: `client.name` вместо `client[1]`. Так код становится читабельнее и проще в использовании.

**Что делаем:**  
Создаём класс `Client`, который будет хранить всю информацию о клиенте: ID, имя, телефон и Telegram. Каждое поле получает своё понятное название.

**Как это работает:**  
1. Используем ключевое слово `class` для создания нового класса.  
2. Создаём специальную функцию `__init__`, которая вызывается при создании нового объекта.  
3. В `__init__` принимаем все нужные данные (id, name, phone, telegram).  
4. Сохраняем эти данные в переменные объекта с помощью `self` — так они будут доступны для каждого конкретного клиента.

**Код:**  
```python
class Client:
    def __init__(self, id, name, phone, telegram):
        self.id = id
        self.name = name
        self.phone = phone
        self.telegram = telegram
```

**Детали реализации:**  
- `self` — это ссылка на сам объект. Через неё мы обращаемся к данным конкретного клиента.  
- `self.id`, `self.name`, `self.phone`, `self.telegram` — это атрибуты объекта, в которых хранятся данные.  
- `__init__` — специальная функция, которая автоматически вызывается при создании нового объекта класса.

**Результат:**  
Теперь у нас есть шаблон для создания клиентов. Каждый клиент будет объектом класса `Client` со своими данными.

---

### 2. Изменение способа хранения клиентов

**Зачем это нужно:**  
Раньше в `client_list` мы хранили списки: `[[1, "Иван", ...], [2, "Петр", ...]]`. Теперь будем хранить объекты класса `Client`. Это удобнее, потому что к данным можно обращаться по понятным именам.

**Что делаем:**  
Вместо добавления списков в `client_list`, теперь создаём объекты класса `Client` и добавляем их в список.

**Как это работает:**  
1. Создаём пустой список `client_list = []` для хранения объектов.  
2. При добавлении нового клиента создаём объект класса `Client` с помощью `Client(id=1, name="Иван", ...)`.  
3. Добавляем этот объект в список с помощью `client_list.append()`.

**Код (старый способ — списки):**  
```python
client_list = [
    [1, "Олег", "+7 993 565 45 78", "@olegnastyle"],
    [2, "Дмитрий", "+7 993 657 45 78", "@dima"]
]
```

**Код (новый способ — объекты):**  
```python
client_list = []

client_list.append(Client(
    id=1,
    name="Иван",
    phone="+7 996 564 87 89",
    telegram="@ivenivanov"
))
```

**Результат:**  
Теперь `client_list` содержит объекты класса `Client`, а не обычные списки. Это делает код более структурированным и понятным.

---

### 3. Обновление функции просмотра клиентов

**Зачем это нужно:**  
Раньше мы обращались к данным через индексы: `client[0]` для ID, `client[1]` для имени. Теперь нужно использовать атрибуты объекта: `client.id`, `client.name`. Это намного понятнее и безопаснее.

**Что делаем:**  
Обновляем функцию `client_read()`, чтобы она использовала атрибуты объекта вместо индексов списка.

**Как это работает:**  
1. Проходим по всем клиентам в `client_list` (теперь это объекты, а не списки).  
2. Обращаемся к данным через атрибуты: `client.id`, `client.name`, `client.phone`, `client.telegram`.  
3. Выводим информацию в том же формате, что и раньше.

**Код (старый способ — индексы):**  
```python
def client_read():
    print("Список клиентов:👯\n")
    for client in client_list:
        print(
            f"ID: {client[0]}\n"
            f"Имя: {client[1]}\n"
            f"Телефон: {client[2]}\n"
            f"Телеграм: {client[3]}\n"
        )
```

**Код (новый способ — атрибуты):**  
```python
def client_read():
    print("Список клиентов:👯\n")
    for client in client_list:
        print(
            f"ID: {client.id}\n"
            f"Имя: {client.name}\n"
            f"Телефон: {client.phone}\n"
            f"Телеграм: {client.telegram}\n"
        )
```

**Детали реализации:**  
- `client.id` — получаем ID клиента (раньше было `client[0]`).  
- `client.name` — получаем имя клиента (раньше было `client[1]`).  
- `client.phone` — получаем телефон клиента (раньше было `client[2]`).  
- `client.telegram` — получаем Telegram клиента (раньше было `client[3]`).

**Результат:**  
Функция работает так же, как и раньше, но код стал понятнее. Теперь сразу видно, какое поле мы используем, не нужно помнить, что индекс 1 — это имя, а индекс 2 — телефон.

---

### 4. Обновление функции добавления клиента

**Зачем это нужно:**  
При добавлении нового клиента нужно создавать объект класса `Client`, а не просто список. Это обеспечивает единообразие — все клиенты будут объектами одного типа.

**Что делаем:**  
Обновляем функцию `client_add()`, чтобы она создавала объект класса `Client` и добавляла его в список.

**Как это работает:**  
1. Вычисляем новый ID для клиента (как и раньше).  
2. Запрашиваем у пользователя имя, телефон и Telegram.  
3. Проверяем на дубликаты по телефону (используя `client.phone` вместо `client[2]`).  
4. Создаём новый объект класса `Client` с помощью `Client(id=..., name=..., phone=..., telegram=...)`.  
5. Добавляем объект в список.

**Код (старый способ — список):**  
```python
def client_add():
    user_last_id = len(client_list) + 1
    user_name = input("Имя: ")
    user_phone = input("Номер телефона: ")
    user_telegram = input("Телеграм: ")

    for client in client_list:
        if client[2] == user_phone:
            print(f"{client[1]} уже есть в базе")
            return
      
    client_list.append([
        user_last_id,
        user_name,
        user_phone,
        user_telegram
    ])
```

**Код (новый способ — объект):**  
```python
def client_add():
    print("Добавление клиента 👨‍👧\n")

    user_last_id = len(client_list) + 1
    user_name = input("Имя: ")
    user_phone = input("Номер телефона: ")
    user_telegram = input("Телеграм: ")

    for client in client_list:
        if client.phone == user_phone:
            print(f"{client.name} уже есть в базе")
            return

    client_list.append(Client(
        id=user_last_id,
        name=user_name,
        phone=user_phone,
        telegram=user_telegram
    ))
    print(f"Клиент {user_name} был добавлен ✔️")
```

**Детали реализации:**  
- `client.phone` — проверяем телефон через атрибут объекта (раньше было `client[2]`).  
- `client.name` — получаем имя для сообщения об ошибке (раньше было `client[1]`).  
- `Client(id=..., name=..., phone=..., telegram=...)` — создаём новый объект класса `Client`.

**Результат:**  
Новые клиенты теперь создаются как объекты класса `Client`, что делает код более структурированным и понятным.

---

### 5. Обновление функции удаления клиента

**Зачем это нужно:**  
При удалении клиента нужно обращаться к его данным через атрибуты объекта, а не через индексы списка. Это обеспечивает единообразие во всём коде.

**Что делаем:**  
Обновляем функцию `client_remove()`, чтобы она использовала атрибуты объекта (`client.id`, `client.name`) вместо индексов (`client[0]`, `client[1]`).

**Как это работает:**  
1. Просим пользователя ввести ID клиента для удаления.  
2. Проходим по всем клиентам в списке.  
3. Сравниваем `client.id` с введённым ID (раньше было `client[0]`).  
4. Если нашли совпадение — удаляем объект из списка и выводим имя клиента через `client.name` (раньше было `client[1]`).

**Код (старый способ — индексы):**  
```python
def client_remove():
    print("Удаление клиента 👤\n")
    user_id_remove = int(input("Введите ID клиента: "))
    user_name_remove = ""

    for client in client_list:
        if client[0] == user_id_remove:
            client_list.remove(client)
            user_name_remove = client[1]
            print(f"Клиент {user_name_remove} был удален ❌")
            return

    print("Такого клиента нет в базе")
```

**Код (новый способ — атрибуты):**  
```python
def client_remove():
    print("Удаление клиента 👤\n")
    user_id_remove = int(input("Введите ID клиента: "))
    user_name_remove = ""

    for client in client_list:
        if client.id == user_id_remove:
            client_list.remove(client)
            user_name_remove = client.name
            print(f"Клиент {user_name_remove} был удален ❌")
            return

    print("Такого клиента нет в базе")
```

**Детали реализации:**  
- `client.id` — получаем ID клиента для сравнения (раньше было `client[0]`).  
- `client.name` — получаем имя клиента для сообщения (раньше было `client[1]`).  
- Логика работы остаётся такой же, меняется только способ обращения к данным.

**Результат:**  
Функция удаления теперь работает с объектами класса `Client`, что делает код единообразным и понятным.

---

### 6. Исправление функции редактирования клиента

**Зачем это нужно:**  
В функции `client_edit()` остался старый код, который пытается обращаться к объекту как к списку (используя `client[0]`, `user[0]` и т.д.). Это вызовет ошибку, потому что объект класса `Client` — это не список, и к нему нельзя обращаться по индексам. Нужно использовать атрибуты объекта.

**Что делаем:**  
Полностью переписываем функцию `client_edit()`, чтобы она работала с объектами класса `Client` через атрибуты, а не через индексы.

**Как это работает:**  
1. Просим пользователя ввести ID клиента для редактирования.  
2. Ищем клиента в списке по ID, используя `client.id` вместо `client[0]`.  
3. Если клиент найден, выводим меню выбора поля для редактирования.  
4. В зависимости от выбора, изменяем нужный атрибут объекта напрямую: `client.name = ...`, `client.phone = ...`, `client.telegram = ...`.  
5. Больше не нужно создавать новый список и заменять весь объект — достаточно изменить нужный атрибут.

**Код (старый способ — со списками, НЕ РАБОТАЕТ с объектами):**  
```python
def client_edit():
    user = []
    user_id = int(input("Введите ID клиента: "))

    for client in client_list:
        if client[0] == user_id:  # ОШИБКА! client[0] не работает с объектами
            user = client
            break

    if len(user) > 0:  # ОШИБКА! len(user) не имеет смысла для объекта
        user_index = client_list.index(user)
        # ... дальше работа со списками
```

**Код (новый способ — с объектами):**  
```python
def client_edit():
    user = None  # Используем None вместо пустого списка
    user_id = int(input("Введите ID клиента: "))

    for client in client_list:
        if client.id == user_id:  # Используем атрибут id
            user = client
            break

    if user is not None:  # Проверяем, найден ли клиент
        print(
            "1 - изменить имя\n"
            "2 - изменить номер телефона\n"
            "3 - изменить телеграм\n"
        )
        value = input("Вы: ")
        print()  # пустая строка
        
        if value == "1":
            user.name = input("Имя: ")  # Изменяем атрибут напрямую
        elif value == "2":
            user.phone = input("Телефон: ")  # Изменяем атрибут напрямую
        elif value == "3":
            user.telegram = input("Телеграм: ")  # Изменяем атрибут напрямую
        else:
            print("Команда не распознана 🤖")
            print("Попробуйте еще раз\n")
    else:
        print("Такого клиента нет в базе")
```

**Детали реализации:**  
- `user = None` — используем `None` вместо пустого списка, чтобы обозначить, что клиент не найден.  
- `client.id` — обращаемся к ID через атрибут объекта.  
- `user is not None` — проверяем, был ли найден клиент (вместо `len(user) > 0`).  
- `user.name = input("Имя: ")` — изменяем атрибут объекта напрямую. Не нужно создавать новый список и заменять весь объект — достаточно изменить нужное поле.

**Результат:**  
Функция редактирования теперь корректно работает с объектами класса `Client`. Можно изменять отдельные поля клиента, и код стал намного проще и понятнее.

---

### 7. Преимущества использования классов

**Почему это лучше, чем списки:**

1. **Понятность кода:**  
   `client.name` сразу показывает, что мы получаем имя. `client[1]` — неочевидно, что это имя, нужно помнить порядок полей.

2. **Безопасность:**  
   Если случайно написать `client.nmae` (с опечаткой), Python сразу покажет ошибку. Если написать `client[10]` (неправильный индекс), ошибка может проявиться только во время выполнения программы.

3. **Удобство редактирования:**  
   Чтобы изменить имя клиента, достаточно написать `client.name = "Новое имя"`. Не нужно создавать новый список и заменять весь объект.

4. **Единообразие:**  
   Все клиенты теперь объекты одного типа. Это помогает избежать ошибок и делает код более предсказуемым.

**Результат:**  
Код стал более структурированным, понятным и безопасным. Работать с данными клиентов теперь намного удобнее.
